CREATE TABLE DNE_TEST(
       datec1c2ecID varchar(32) PRIMARY KEY,
       dates date,
       country_name1 varchar(255),
       country_name2 varchar(255),
       event_code varchar(32),
       event_name varchar(255),
       event_code_count integer
);
